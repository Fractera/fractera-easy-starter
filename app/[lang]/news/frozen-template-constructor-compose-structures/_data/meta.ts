import { AUTHOR } from '@/lib/author'
import type { NewsArticleMeta } from '../../_lib/types'

export const meta: NewsArticleMeta = {
  slug: 'frozen-template-constructor-compose-structures',
  date: '2026-06-26',
  readingMinutes: 7,
  tags: ['Agentic Engineering Platform', 'Frozen Template Constructor', 'No Code Generation', 'Composition', 'MCP'],
  author: { name: AUTHOR.name, role: AUTHOR.role },
  // Published without a bespoke illustration — the brand logo stands in as the OG
  // snippet; a real diagram (the basis → composer → structure) can replace it later.
  ogImage: '/fractera-logo.jpg',
}
